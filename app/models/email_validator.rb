class EmailValidator < ActiveModel::Validator
  def validate(record)
    record.email (options[:message] || "is not an email") unless
      value =~ /\A([^@\s]+)@((?:[-a-z0-9]+\.)+[a-z]{2,})\z/i
  end
end